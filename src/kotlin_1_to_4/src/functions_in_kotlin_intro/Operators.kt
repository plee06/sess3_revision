package functions_in_kotlin_intro

/*
* Operators in Kotlin are functions that use a symbolic name. Consider the + * ...
*
* Array access is also an operator, it's a real function!!!
*
*
* */

// the [0] operation of some Array is a call to the function get(index: Int) which is translated
// of course this .get method is defined in the Array class in Kotlin