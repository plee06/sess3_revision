package oop_kotlin

// Like in Java, interfaces can be declared in Kotlin. Interfaces are of course, nothing more than a contract

// Implementer of the interface must adhere to the methods we put in the interface by implementing them

// Interfaces can contain properties and fun declarations, concept is the same as Java ... we won't be getting
// tested on Kotlin so just be familiar with this