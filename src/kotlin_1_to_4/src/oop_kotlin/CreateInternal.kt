package oop_kotlin

fun main(args: Array<String>)
{
    // I can do this because the class is declared public
    val pub: PublicClassYo = PublicClassYo()

    // I can also do this because the class is "internal"
    val inter: InternalClassYo = InternalClassYo()


}